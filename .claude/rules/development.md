---
description: How to run the project locally and development constraints
alwaysApply: true
---

# Development

Serve locally with any static file server:

```bash
python3 -m http.server 8080
# or
npx serve .
```

- No build tools, no package manager, no test suite
- No linting, no CI, no automated tests
- Open HTML files directly in a browser or via the local server above
